# feelzlike — Weather Intelligence Platform

## Overview

**feelzlike** is a global weather intelligence platform offering detailed snow and green season intelligence for various destinations. The project provides regional sub-applications under a unified `feelzlike` umbrella, starting with Japan and Australia. The main `feelzlike` application serves as a landing page and region selector, while sub-applications (e.g., `yamanouchi`) deliver localized, bilingual intelligence. The business vision is to provide a comprehensive weather intelligence solution, with market potential in tourism and outdoor sports industries.

## User Preferences

- **Communication style**: The project documentation should be clear and concise.
- **Coding style**: The project uses pnpm workspaces with TypeScript, React, Vite, and Tailwind CSS.
- **Workflow**: Iterative development is preferred, with a focus on delivering functional features for each region.
- **Interaction**: Prioritize architectural decisions over granular implementation details. Avoid making changes to areas not explicitly specified by the user.
- **General working preferences**: The user prefers a light and clean design aesthetic, inspired by snow-forecast.com, with a consistent typography across all applications. All applications should share a unified design system. The user wants to see a town-centric information architecture, where each region is anchored on a single base town.
- **REGION SHAPE — HARD RULE (every new region, no exceptions)**: Always lead with **towns**, then **mountains hang off each town**. The region home (`/:region/`) is a town picker. A town card surfaces the mountain(s) that town serves. Mountain detail is reached *via a town*, never as a flat top-level resort list. This is the basis for the app — confirm with the user before deviating, and remind the user of this rule when scoping a new region. See `RegionConfig` in `lib/feelzlike-shell/src/types.ts` for the data shape; the existing Snowy Mountains and Yamanouchi configs are the reference implementations.

- **MOUNTAIN DETAIL CANONICAL FORMAT — HARD RULE (every region, no exceptions)**: All three region "mountain detail" pages MUST render the same sections in the same order, with the same gating, using the SAME shared components. Reviewed and reconciled May 2026. To add a new region, copy this skeleton — do NOT invent a new section order.
  - **Files**: AU Snowy → `regions/snowy-mountains/pages/LocationDetail.tsx`. JP → `regions/yamanouchi/pages/resort.tsx`. AU Vic + any new region with no custom layout → `pages/region/MountainDetail.tsx` (the generic).
  - **Canonical section order** (top → bottom):
    1. Page header / hero
    2. **Hourly forecast** (free, next 48h, with Powder Window detector) — `<HourlyForecast>` from `@/components/HourlyForecast`. Must pass `thresholds={POWDER_THRESHOLDS_AU}` for AU regions; JP defaults to Japow thresholds in the component.
    3. Premium · Next 6 days
    4. Premium · **Elevation forecast** — `<ElevationBands>` from `@/components/weather/ElevationBands` (shared, mobile = stacked day cards, md+ = 5-col table). Self-hides when coords/elevation missing; gate with `hourly.length > 0 && location?.elevation != null`.
    5. **PowderCalendar** (free, sits AFTER ElevationBands) — `<PowderCalendar>` from `@/components/PowderCalendar`. AU passes `thresholds={POWDER_THRESHOLDS_AU}`.
    6. Premium · Mountain dials (MountainSnapshot rings)
    7. Premium · **Per-lift hold forecast** — `<LiftWindHoldPanel>`. **MUST be page-level gated** by `getLiftsForMountain(<mountainId>).length > 0` so free users never see a lock card for a mountain with no lift seeds. PremiumGate alone is not enough — it still renders the lock chrome even when its child returns null.
    8. Premium · **24h trend chart** — `<ForecastChart>` from `@/components/weather/ForecastChart` (shared).
    9. Premium · **Ensemble forecast** — `<EnsembleForecast>` from `@/components/weather/EnsembleForecast` (shared).
    10. Premium · Alerts (paywall card linking to `~/<region>/alerts`)
    11. **Webcams** (free, last) — `<MountainWebcams>` from `@/components/MountainWebcams`.
  - **Shared vs region-local**: `ElevationBands`, `ForecastChart`, `EnsembleForecast`, `HourlyForecast`, `PowderCalendar`, `LiftWindHoldPanel`, `MountainWebcams` ALL live under `artifacts/feelzlike/src/components/` and are imported from `@/components/...`. Do NOT fork these into region folders — region-local copies (and re-export shims) are an anti-pattern that already cost us a sync drift bug.
  - **PowderFactorBadge is fully removed.** Do not reintroduce it.
  - **No em/en dashes anywhere.** Only middot ·. Lowercase brand. (Project-wide rule, repeated here for the gate copy/blurbs that frequently get authored fresh.)

- **TOWN HOME "Weather in mountains" PANEL — HARD RULE**: The mountain list on each base-town page (`pages/region/TownHome.tsx`) renders each resort with a per-resort colour tint defined in the local `MOUNTAIN_TINTS` map (keyed by mountain id, or by parent group id for umbrella rows like Shiga Kogen / Kita Shiga). Tints stay inside the sky/blue brand family (sky · indigo · cyan · blue · teal · slate). When adding a new mountain, add a new entry to `MOUNTAIN_TINTS` — the `FALLBACK_TINT` exists only as a safety net.

- **ADDING A NEW REGION — CHECKLIST**: (1) Create `src/regions/<region-id>.ts` mirroring `snowy-mountains.ts` / `yamanouchi.ts` (mountains array with id/name/elevationM/lat/lng/blurb/websiteUrl, baseTowns array, parentId for umbrella groups); (2) register in `src/regions/index.ts` `REGIONS` and assign country in `REGION_COUNTRY`; (3) add region to `RegionLayout.tsx` `REGION_ROUTERS` ONLY if it needs custom pages — otherwise generic `pages/region/*` handles it; (4) add server-side: `artifacts/api-server/src/lib/regions.ts` `REGION_IDS` + `LOCATION_TO_REGION`, weather entries in `routes/weather.ts`, regions descriptor in `routes/regions.ts`; (5) update `lib/api-spec/openapi.yaml` `RegionId` enum and regen `pnpm --filter @workspace/api-zod run codegen && pnpm --filter @workspace/api-client-react run codegen` then `tsc -b lib/api-client-react --force`; (6) seed `data/lifts.ts` and `data/webcams.ts` for each mountain (or skip — page-level gates self-hide); (7) add per-resort tints to `MOUNTAIN_TINTS` in `pages/region/TownHome.tsx`; (8) add headline to `landing.tsx` `FALLBACK_REGIONS` + `routes/regions.ts` `headlineLabel`. The mountain detail page renders for free via the generic `pages/region/MountainDetail.tsx` because routing is data-driven (`region.mountains.find()`, not a hardcoded id whitelist).

## System Architecture

The project is a pnpm workspace monorepo using Node.js 24 and TypeScript 5.9.

**Core Technologies:**
- **Frontend:** React, Vite, Tailwind CSS, Framer Motion, React Leaflet.
- **Backend:** Express 5.
- **Database:** PostgreSQL (via Replit) with Drizzle ORM for local data, and Supabase for live snow/resort/storm data.
- **Validation:** Zod (`zod/v4`), `drizzle-zod`.
- **API Codegen:** Orval (from OpenAPI spec).
- **Build:** esbuild.

**Monorepo Structure:**
- `artifacts/api-server`: Express API server for snow and local places data.
- `artifacts/feelzlike`: The unified application serving the region picker and all regional content. Regional content is organized under `src/regions/<region>/`.
  - **Information architecture is 3-tier**: `Regions → Base Towns → Mountains`.
    - `/:region` — Region overview (region weather + base-town cards + mountain cards).
    - `/:region/:town` and `/:region/:town/{roads,cams,transport,stay,eat,explore}` — town-life pages, scoped to the selected base town. Routed via a nested `TownLayout` (Wouter base prefix).
    - `/:region/mountains`, `/:region/mountain/:id` (legacy `/resort/:id` retained), `/:region/today`, `/:region/radar`, `/:region/alerts` — mountain/region-wide pages.
    - `/:region/today` — **Today's call**: composite-scorer comparison page that ranks every mountain in the region (winter weights: snow 40 / wind 25 / temp 20 / cloud 15; green-season variant flattens snow). Renders a winner card (ScoreDial + 4 stat tiles + journey pill) and a numbered ranked list with tone-coded badges (POWDER DAY / BLUEBIRD / FAIR / MARGINAL / NO-GO). Reads from `entry.current.*` on `/api/weather`. Surfaced via a CTA card on the region overview and a sidebar item.
  - Reserved town slugs (cannot collide with town ids): `mountain`, `mountains`, `radar`, `alerts`, `resort`.
- `lib/feelzlike-shell`: Shared components for region chrome (e.g., `AppShell`, `RegionProvider`, `SeasonProvider`, `LanguageProvider`, `BaseTownProvider`, `TownPicker`, `RegionConfig`). Implements a light "premium fintech" visual language. AppShell renders three sectioned nav blocks (Region / In Town / Mountains); `defaultNav.ts` exports `DEFAULT_TOWN_NAV`, `DEFAULT_MOUNTAIN_NAV`, `DEFAULT_REGION_NAV` which regions can override via `RegionConfig.navOverrides`. `TownPicker` is URL-driven and navigates between towns while preserving the current sub-route.
- `lib/feelzlike-dashboard`: Shared dashboard component package for consistent UI across regional resort detail pages.
- `lib/api-spec`: OpenAPI 3.1 specification and Orval configuration.
- `lib/api-client-react`: Generated React Query hooks and fetch client.
- `lib/api-zod`: Generated Zod schemas for API validation.
- `lib/db`: Drizzle ORM layer for PostgreSQL.
- `lib/integrations-anthropic-ai`: Anthropic AI client.

**UI/UX Decisions:**
- **Design System:** Unified light and clean aesthetic across all applications, inspired by snow-forecast.com.
  - **Color Palette:** Sky blue (`--primary`) and alpine orange (`--accent`).
  - **Typography:** DIN Pro for all text, with Noto Sans JP fallback for Japanese glyphs.
  - **Layout:** Desktop features a 264px white sidebar; mobile uses a `glass-strong` top header and bottom navigation.
- **Town-centric IA:** Each region is anchored on a base town, influencing navigation and content display.
- **Season Toggle:** Automatic season detection (Winter/Green) with manual override, adapting UI elements, navigation, and content.

**Feature Specifications:**
- **Master Landing Page:** Features an editorial hero, region search, and town-first region cards with live headline data from `/api/regions`. Includes a trust line with real-time update information.
- **Aggregate `/api/regions` endpoint:** A new API route aggregating Open-Meteo headline and 6-day daily forecasts for key regional locations, with robust caching and error handling.
- **Region-aware multi-region API endpoints:** `/api/weather`, `/api/webcams`, `/api/road-conditions`, `/api/lift-status` and `/api/alerts` accept an optional `?region=snowy-mountains|yamanouchi|iiyama` query param. Region IDs are canonical (matching `/api/regions`) and validated server-side via the shared `parseRegionParam` helper in `artifacts/api-server/src/lib/regions.ts`, which holds the master `LOCATION_TO_REGION` map. `parseRegionParam` rejects non-string and inconsistent array inputs, throwing `RegionParamError` (HTTP 400 `INVALID_REGION`). Omitting the param returns the full multi-region payload (backward-compatible). Endpoints lacking data for a requested region (e.g. JP roads, JP lifts, AU alerts) return an empty list rather than 404 so the client renders a clean empty state; the `X-Empty-Reason` response header explains why (and `/road-conditions` additionally surfaces a `generalAdvice` message in the body). The OpenAPI spec exposes this as a reusable `RegionFilter` parameter component and a `RegionId` enum schema; orval-generated React Query hooks accept `{ region }` as the first arg. All `feelzlike` callers pass region — shared pages use `useRegion().region.id` and region-specific pages pass a literal.
- **Production reliability layer (`/api/regions`):** Implements a stale-while-revalidate cache (5-min fresh, 6-hour stale) with request coalescing to ensure high availability and responsiveness.
- **Health probes:** `/api/healthz` for status and `/api/readyz` for readiness checks, ensuring traffic is routed only when the service is fully operational.
- **Rate limiting:** `express-rate-limit` middleware on all API routes, preventing abuse with configurable trust-proxy hops.
- **Reusable LRU+TTL cache helper:** Provides a generic caching mechanism for future per-town/per-mountain data.
- **Paywall scaffolding:** Database schema for users and subscriptions, and an entitlement system (`lib/entitlements.ts`) to manage feature access based on subscription tiers. Includes a `requireEntitlement` middleware for server-side access control.
- **Consent + ad-tracking framework:** Provider-agnostic GDPR/IAB-style consent with essential, analytics, and ads categories, persisted to localStorage. Features a customizable banner for user choice.
- **Sentry error monitoring:** Integrated across all artifacts (API server and SPAs) for comprehensive error tracking, performance monitoring, and session replay, with artifact-specific tagging and source map upload for production builds.
- **Snow-forecast.com-style 6-day strip:** Detailed 6-day forecast display with weather icons, temperatures, snowfall, rainfall, wind speed, and sunrise/sunset times.
- **Yamanouchi App:** Live snow data, accommodation/dining/attraction listings, and a season toggle influencing UI and API calls.
- **Iiyama App:** A "coming-soon" page with editorial content and CTAs to other regions.
- **Snowy Mountains App:** Primary navigation for Weather, Cams, Radar, Roads, Lifts, and secondary navigation for Bus Services, including a dashboard hero with live BOM Australia data.
- **Today's Call (fintech matrix):** Region overview hero shows top-3 mountains as score tiles with live mini-stats and drive time; full `/today` page renders a winner banner (score dial + Trophy + Resort website CTA) and a side-by-side comparison matrix with per-column ▲ winner markers (Snow / Wind / Temp / Cloud / Drive). Mountain coords are sourced from `region.mountains[].lat/lng` (single source of truth — no hardcoded coord maps).
- **Data-driven mountain detail routing:** `/[:region]/mountain/:id` is gated by `region.mountains.find()` (not a hardcoded `VALID_IDS` whitelist), so adding a mountain to a region config makes it routable, scoreable in Today's Call, and renderable on the detail page in one place. Resort lift-status / webcam URLs remain in a curated `PROFILES` override map; website URL is auto-sourced from `mountain.websiteUrl`. Link tiles render only when their URL exists — no fake placeholders.
- **Yamanouchi 22-mountain refactor (Option B, May 2026):** Today's Call now ranks every individual mountain in the region instead of treating Shiga Kogen as one umbrella. `regions/yamanouchi.ts` lists 22 mountains: 18 Shiga Kogen sub-areas (slugs `shiga-{sun-valley,maruike,hasuike,giant,hoppo-bunadaira,higashidateyama,nishidateyama,terakoya,takamagahara,tannenomori-okojo,ichinose-family,ichinose-diamond,ichinose-yamanokami,yakebitaiyama,okushiga-kogen,kumanoyu,yokoteyama,shibutoge}`, all with `parentId: "shiga-kogen"`) + 4 Kita-Shiga resorts (`ryuoo`, `xjam-takaifuji`, `yomase-onsen`, `kita-shiga-komaruyama`). The umbrella `shiga-kogen` mountain entry was dropped — `parentId` is retained purely as a logical key so `useTodaysWinner.ts` continues to roll drive-time data up to the parent `shiga_kogen` curated key in stay.drive_min_to_each_mountain. Touched 8 files: `regions/yamanouchi.ts`, `data/lifts.ts` (25 lifts), `data/webcams.ts` (14 cams — 8 sub-areas share the Shiga authority cam page via fallback), `pages/landing.tsx`, server `lib/regions.ts`, `routes/weather.ts` (22 single-line entries), `routes/snow.ts` FALLBACK_RESORTS, `lib/api-spec/openapi.yaml` enum (regenerated api-zod + api-client-react). FE deep-link maps were updated to match: `regions/yamanouchi/pages/resorts.tsx` REGION_LIVE_MAP now points to representative slugs (Yakebitaiyama for Shiga Kogen group, Ryuoo for Kita-Shiga group); `regions/yamanouchi/pages/resort.tsx` PROFILES was refactored to derive Shiga lift-status + webcam defaults from `mountain.parentId === "shiga-kogen"` with explicit overrides only for the 4 non-Shiga resorts — adding more Shiga sub-areas now requires zero PROFILES edits. Tighter town POI radii (Yudanaka 700m, Shibu 400m) plus a hard `radius * 1.05` cap in `places.ts` stop Eat/Stay duplication between adjacent towns; JP road-cam pages collapse to an honest "open the official road-camera map" tile when no genuine deep-links exist (Hokushin source has none).
- **Iiyama temporarily paused (May 2026):** While shipping Snowy Mountains and Yamanouchi to v1.0 we removed Iiyama from the active region set so we can focus QA, monitoring, and design polish on just the two regions that actually have curated data. The removal is intentionally surgical and reversible: source files (`artifacts/feelzlike/src/regions/iiyama.ts`, the entire `artifacts/feelzlike/src/regions/iiyama/` directory) are kept on disk so the region can be re-enabled by reverting the unwiring. Touched: `artifacts/feelzlike/src/regions/index.ts` (removed iiyama from REGIONS), `src/layouts/RegionLayout.tsx` + `src/layouts/TownLayout.tsx` (removed iiyama from REGION_ROUTERS), `src/pages/landing.tsx` (removed iiyama from FALLBACK_REGIONS + 2 copy mentions), `src/data/transport/index.ts` (removed iiyama key from the strictly-typed `Record<RegionId, …>` REGISTRY). Server-side: `artifacts/api-server/src/lib/regions.ts` (removed from `REGION_IDS` and `LOCATION_TO_REGION`), `routes/regions.ts` (removed iiyama region descriptor), `routes/weather.ts` (removed `madarao` / `tangram` / `togari` LOCATIONS — also stops 3 wasted Open-Meteo calls per `/api/weather` request), `routes/webcams.ts` (removed `iiyama-roads` entry). Schema: `lib/api-spec/openapi.yaml` (removed iiyama from `RegionId` enum), regenerated `api-zod` + `api-client-react`, then `tsc -b lib/api-client-react lib/api-zod --force` to refresh the composite `.d.ts` files (project references mean consumers read from `dist/`, not `src/`, so the schema regen alone is not enough — the rebuild step is mandatory). **Caveat — `lib/api-zod/dist` could NOT be rebuilt** because pre-existing duplicate-export errors between `./generated/api` and `./generated/types` block `tsc` emit; the api-zod dist `.d.ts` has therefore been stale long before this work (this is unrelated, pre-existing debt). Runtime is unaffected because the api-server consumes api-zod from `src/` directly via the package's `exports` field — only TypeScript project-reference typechecking against api-zod sees the stale dist, and those errors (`GetBusServicesResponse`, `GetLiftStatusResponse`, `GetAccommodationResponse`, etc.) are entirely unrelated to RegionId/iiyama. Fix is a separate task: dedupe `api-zod/src/index.ts` re-exports. Verified: landing page renders only Snowy Mountains + Yamanouchi cards (screenshot), `/iiyama` correctly falls back to the picker via the existing `<Redirect to="/" />` in RegionLayout. Each removal point has a `// Iiyama temporarily removed — see …` breadcrumb pointing back to `lib/regions.ts` so re-enabling is a guided diff.

- **Hour-by-hour 48h forecast + Powder Window (Sprint 1, Prompt 1.4):** Mountain detail pages previously showed daily 6-day forecasts only; skiers need hourly resolution to plan when to be on the chair (snow timing, wind windows). Built `artifacts/feelzlike/src/components/HourlyForecast.tsx` — a horizontal scroll-snap strip of fixed-width hour tiles (NOW, time, weather icon, temp, snowfall if any, wind), plus a Powder Window detector that finds the longest consecutive run of hours meeting `snowfall ≥ Xcm/hr AND wind < Ykm/h AND duration ≥ 3h` and renders a sky-blue Sparkles badge labelled e.g. "9am–12pm · 8cm forecast" alongside an in-strip highlight on the qualifying tiles. Thresholds are configurable per consumer via `POWDER_THRESHOLDS_DEFAULT` (Japow: 1cm/hr, <20km/h) and `POWDER_THRESHOLDS_AU` (relaxed: 0.5cm/hr, <25km/h — AU rarely hits Japow benchmarks per playbook safety net) exported from `artifacts/feelzlike/src/types/weather.ts` along with `detectPowderWindow()`. Component filters hourly to hours from now onward, slices to 48, and renders nothing if no future hours exist; if there's some snow but no qualifying window, an italic empty-state line appears below the strip. Uses native `overflow-x-auto + snap-x snap-mandatory` (no Embla — simpler, smaller bundle, equally smooth on mobile). Time strings come back from Open-Meteo as naive ISO in the location's local timezone, so date-fns `format("ha")` produces the correct local hour without conversion (JST for JP, AEDT for NSW). **Server-side prerequisite:** the `LocationWeather.hourly` array used to be either past-24h BOM observations OR next-72h Open-Meteo forecast (mutually exclusive). Patched `artifacts/api-server/src/routes/weather.ts` to MERGE both — BOM past observations dedupe by hour-string against the OM future forecast, so a single `hourly` array now serves both the existing 24h-trend chart (past) AND the new "Next 48h" strip (future). Wired into all 3 mountain detail pages: `snowy-mountains/pages/LocationDetail.tsx` (section `04b`, AU thresholds), `yamanouchi/pages/resort.tsx` and `iiyama/pages/resort.tsx` (section `04`, default thresholds, `t` translator passed for EN/JA). Verified on Shiga Kogen (JP, 13 future hours, no PW), Madarao (JP, 13 future hours, no PW), Thredbo (AU, BOM-past + OM-future merged hourly with empty-state copy correctly visible). Architect review caught one critical issue — naive ISO timestamps were being parsed in the *viewer's* browser timezone for the past/future cutoff comparison, so an Australian browsing Shiga Kogen would see a 2h-shifted "NOW" tile and incorrect Powder Window boundaries. Fixed by adding `utcOffsetSeconds` (integer, optional) to the `LocationWeather` OpenAPI schema (regenerated `api-zod` + `api-client-react`), populating it server-side from `om.utc_offset_seconds`, and using it in the component to compute "now in the resort's local wall clock" then comparing against each hour's "fake-UTC ms" parse — same pattern already used by the server's `sumHourlySnowfall`. Display labels now slice the hour digits straight out of the ISO string (`time.slice(11,13)` → "5pm") so the wall-clock label is always the resort's local hour regardless of viewer TZ. Strip is also keyboard-focusable (`tabIndex=0` + aria-label) per architect a11y note.
- **Branded empty-state card (Sprint 1, Prompt 1.3):** Bare "No X found" / "Coming soon" plain text across the app was reading as broken rather than as anticipated. Built a reusable `EmptyStateCard` at `artifacts/feelzlike/src/components/EmptyStateCard.tsx` with props `{ icon: LucideIcon, title, body, eta?, ctaLabel?, ctaHref? }` — centered card on `bg-white`, hairline `border-border`, `rounded-2xl`, sky-blue accented icon chip (`bg-primary/10 text-primary`), `font-display` headline, optional ETA pill (Clock4 + uppercase tracking), optional shadcn `Button asChild` CTA (mailto stays in-tab; http(s) opens new tab w/ `noopener noreferrer`). A11y: `role` is an opt-in prop (`"status" | "region" | "alert"`) — defaults to undefined since the inner `<h2>` is sufficient landmark in most cases; pass `role="status"` only when the card appears post-fetch and should be announced as a live region. Framer-motion entrance matches the rest of the app. Wired into 6 surfaces: (a) `TownStay` empty → "Stays launching this week" + 7-day ETA + `StayPlatformBar` (8-site booking comparison) directly below — top platform bar is hidden in the empty state so the card owns the moment and the bar renders exactly once; (b) `TownPlaces` with `kind="eat"` → "Eats launching shortly" + 14-day ETA + "Suggest a spot" mailto CTA with subject pre-filled `Eat suggestion · {region} · {town}` — the placeholder address lives in a single grep-able `FEEDBACK_EMAIL` constant at the top of `TownPlaces.tsx` with a `TODO(feelzlike-launch)` marker, so swapping for the real inbox before public launch is one edit; (c) `TownPlaces` with `kind="explore"` → "Nothing nearby just yet" with no ETA (real-data-empty, not launching); (d) `RegionStay` empty → same "Stays launching this week" pattern; (e) `TownTransport` empty (only Iiyama right now per Prompt 1.1) → "Transport launching soon" + sprint ETA; (f) `TownRoads` two surfaces — `!dataAvailable` becomes "Live road data coming soon" with the official source as a CTA button (e.g. "Japan Road Traffic Information Center (JARTIC)"), and the no-advisories empty becomes a reassuring "All clear" card (body copy explicitly warns conditions change quickly in winter to avoid false reassurance). Full EN/JA via the existing `t(en, ja)` hook. Verified live across all 3 regions; the conditional empty-state branch correctly defers to the real-data render whenever data exists (Jindabyne Eat still shows 8 restaurant cards; Yudanaka Eat shows the launching card).

- **Explore page resilience (Sprint 1, Prompt 1.2):** The town-level Explore page (`/:region/:town/explore`, served by `TownPlaces.tsx`) was hanging on its loading skeleton because `/api/places/nearby?kind=explore` returned HTTP 502 — Google Places (New) `places:searchNearby` rejected the legacy v1 type `natural_feature` (not in Table A) with `INVALID_ARGUMENT`. Fixed in `artifacts/api-server/src/routes/places-google.ts` by swapping `natural_feature` for the valid Table A types `national_park` + `hiking_area` (covers the same outdoor/nature intent); added a comment block citing Google's type-table-A page so future edits don't reintroduce the regression. Telemetry: `lib/places.ts` `fetchNearby()` now wraps the fetch in try/catch and calls `Sentry.captureException` with tags `{ feature, kind, status, source }` plus a breadcrumb (`places.fetch.network_error` / `places.fetch.http_error`) for both transport-level failures and non-2xx responses, so the same class of upstream regression will be caught in production telemetry next time. Error UI: `TownPlaces.tsx` replaces the bare error text with a friendly card (AlertCircle icon, headline + body copy in EN/JA, technical message in muted byline, `role="alert"`) and a "Try again" Button bound to `query.refetch()` (uses the existing shadcn Radix `Button`; shows a spinning `RefreshCw` + "Retrying…" while `query.isFetching`). Adjacent fix: town `radiusM` is set tight (Yudanaka 700m, Shibu 400m) for Stay/Eat to prevent neighbour-overlap, which silently filtered out Yudanaka's iconic regional destinations (Jigokudani Monkey Park is ~3km away). `TownPlaces` now applies a kind-aware floor — `Math.max(town.radiusM, 6000)` for `kind === "explore"` — so Stay/Eat keep the tight radius while Explore surfaces the regional POIs people actually base their stay around. Verified live: Yudanaka explore now shows World Peace Kannon Statue + Kaede Hot Spring + Yudanaka Onsen Pudding Honpo; Iiyama explore shows the Snow Festival + Iiyamajō Castle Ruins.

- **Region-isolated transport data layer (Sprint 1, Prompt 1.1):** Each Transport provider is a typed `TransportProvider` (id, type, operator, phone, website, route_summary, optional `_local` kanji variants, `schedule_url`, and a required `regions: RegionId[]`). Per-region files at `src/data/transport/{snowy-mountains,yamanouchi}.ts` are aggregated by `src/data/transport/index.ts`, which runs a module-load integrity check (throws at startup if any provider lacks self-reference in its registered region). Phone numbers and websites are `null` where not directly verifiable — never guessed. `src/lib/regionGuard.ts` exports `assertProvidersForRegion(records, regionId, {source, page})` — in dev it throws on the first leak, in prod it logs to console, drops a Sentry breadcrumb, captures an exception, and returns the safe-filtered subset. `src/pages/town/TownTransport.tsx` was refactored to read from the per-region data and run the guard, replacing the previous `useGetBusServices()` hook that returned NSW-only data globally (the v0.3 leak that surfaced "Cooma Coaches" on `/yamanouchi/yudanaka/transport`). Yamanouchi additionally provides a custom rich Transport page (winter timetables + kanji sections) wired via the `RegionRouter.Transport` override pattern: `RegionLayout` declares `Transport?: () => ReactElement | null`, `yamanouchiRouter` exports it, and `TownLayout` looks up `REGION_ROUTERS[region.id]?.Transport` and falls back to `TownTransport`. Iiyama has no curated transport yet → renders the "launching soon" empty state. The `/api/bus-services` endpoint is now orphaned (kept alive but unused). The same audit confirmed Stay/Eat/Explore are geo-isolated (Google Places by lat/lng) and Roads/Cams already pass `?region=`, so Transport was the only true leak surface.

- **Powder-alert subscription system (Sprint 5, Prompts 5.1–5.4):** Turned the read-only storm tracker into a full subscription product (email + web push + cron + management). **Schema** (`lib/db/src/schema/alerts.ts`, 3 tables, pushed to DB): `alert_subscribers` (email, regions[], mountains[], snowfall_threshold_cm, horizon_hours, delivery, timezone, verified_at, unsubscribed_at, last_alerted_at), `alert_push_subscriptions` (one row per browser/device, FK cascade to subscriber, holds endpoint+p256dh+auth), `alert_dispatched` (audit + dedupe log keyed by `alert_window=region:YYYY-MM-DD`). **Auth**: stateless HMAC tokens (`<b64url(payload)>.<b64url(sig)>`) with kinds `verify` (7d), `manage` (90d), `unsub` (no-exp) — secret is `ALERT_TOKEN_SECRET` env var with ephemeral dev fallback (`artifacts/api-server/src/lib/alertTokens.ts`). **Email**: direct fetch to api.resend.com (no Resend integration available — checked) with branded HTML templates (verification + powder alert, navy on white, sky-blue accents) at `lib/emailTemplates.ts` and a console-log fallback when `RESEND_API_KEY` is unset (`lib/emailSender.ts`). **Push**: web-push package + VAPID keypair (`VAPID_PRIVATE_KEY` secret + `VITE_VAPID_PUBLIC_KEY` public env), service worker at `artifacts/feelzlike/public/sw.js` (showNotification with tag-based dedupe and click-to-open). Frontend helper at `src/lib/pushSubscribe.ts` includes iOS PWA detection (`navigator.standalone`) — Safari only allows push when app is added to home screen. **Endpoints** (5 public + 1 internal, all under `/api/alerts/`): POST /subscribe (upsert by email, sends verification email, returns `devVerifyUrl` in dev), GET /verify (sets verified_at, returns manage token), GET+PUT /manage (read/update prefs via manage token), POST /unsubscribe (manage OR unsub token, sets unsubscribed_at + reason), POST /push/subscribe + DELETE /push/subscribe (manage token gated). Internal: POST /api/internal/alerts/run (Bearer-auth in prod, open in dev) for cron triggers. **Cron evaluator** (`artifacts/api-server/src/jobs/alertEvaluator.ts`): node-cron `0 */3 * * *`, fetches one ensemble forecast per region anchor (snapshot reuse — no per-subscriber upstream calls), iterates active subscribers, picks best matching region (snowSum over horizon_hours/24 days ≥ threshold), enforces 12h per-subscriber rate limit, 22:00–06:00 quiet hours in subscriber's timezone (overridden when forecast ≥50cm), dedupes against `alert_dispatched` within last 24h, dispatches email + per-device push, deletes 410-Gone push endpoints, writes audit row with success+error, updates `last_alerted_at`. Returns a structured report (`subscribersChecked / alertsSent / errors / skipped:{dedupe,rateLimit,quietHours,belowThreshold}`). For Replit Deployments where the api-server may be ephemeral, set `ALERT_CRON_DISABLED=1` and use a Scheduled Deployment hitting the internal endpoint instead. **UI**: `AlertSubscribeForm` (region multi-select chips, snowfall slider 5–100cm, 24/48/72h horizon, email/push/both delivery, consent line) wired onto `/snowy-mountains/alerts` and `/yamanouchi/alerts`; top-level pages `/alerts/{verify,manage,unsubscribed}` mounted in App.tsx **above** the `/:region` catch-all (else they'd be intercepted as region slugs). Manage page renders region picker, threshold/horizon/delivery edit, browser-notification enable/disable button (calls `ensurePushSubscription` with manage token), unsubscribe-with-reason flow. Verified end-to-end via curl (subscribe→verify→manage→update→unsubscribe→cron-run-with-dedupe) plus screenshot of the Manage page. **OpenAPI**: 5 public endpoints + 7 schemas added to `lib/api-spec/openapi.yaml`, orval regenerated, `lib/api-client-react/dist` rebuilt — exposes `useSubscribeToAlerts`, `useVerifyAlertSubscription`, `useGetAlertPreferences`, `useUpdateAlertPreferences`, `useUnsubscribeFromAlerts` hooks. Push endpoints intentionally use raw fetch (client-only, never called from generated code).

- **Wired Eat page (Sprint 3, Prompts 3.1–3.5):** `artifacts/feelzlike/src/pages/town/TownEat.tsx` is the curated Eat surface for all 6 base towns (121 entries), mirroring TownStay's chrome (region guard, sticky filter bar, list/map toggle slot, URL-driven detail sheet, 60s tick re-sort). Five new modules support it. **(1) `src/components/EatCard.tsx`** — horizontal card with type chip (Restaurant/Cafe/Bar/Pub/Bakery/Izakaya), cuisine chips capped at 3 with "+N" overflow, 4-dot price band, region-aware CriticalBadges, and an `OpenNowPill` that re-renders itself off `isOpenNow().nextChange`. **(2) `src/components/EatFilterBar.tsx`** — 12 URL-owned keys (`type/cuisine/price/sort/open/apres/take/groc/en/pay/veg/kid`) with default sort `open_first`; same `setUrlParams` plural-write pattern from Sprint 2 to avoid back-stack churn. **(3) `src/lib/openNow.ts`** — zero-dep timezone-aware open/closed classifier. Timezone is **derived from `eat.country`** (AU=Australia/Sydney, JP=Asia/Tokyo) — no per-eat tz field needed since every AU eat is NSW and every JP eat is Nagano. Parser tolerates en-dash / em-dash / hyphen separators in hours strings, handles overnight ranges by yesterday-bleed (a Tuesday 22:47 query against a "17:00–00:00" Mon entry walks back to Monday's range and bumps closeMin to 1440), and looks 7 days forward for `nextChange` re-render scheduling. `last_order_time` is intentionally **not used** for status — the curated data has too many free-form variants (`"when soba sells out"`, `"13:30 (Dec-May) / 14:30 (Jun-Nov)"`) to parse robustly; it's surfaced as detail-sheet text only. Closing-soon threshold = 30 min. **UX bug fix mid-sprint:** `formatMinuteOfDay(1440)` was rendering as `"00:00"` (mod-wrap to 0), making AU pubs that close at midnight (e.g. Bacco's `17:00–00:00`) read as "Open · until 00:00" — visually parsed as "closed already". Fixed by special-casing `norm === 0` to render as `"midnight"`. **DST hardening (architect-flagged):** status classification is always correct because it's derived via `Intl.DateTimeFormat` on each call, but `nextChange` is computed in TZ-minute space (`now + deltaMinutes * 60000`) which can drift by ~1 hour around AEDT↔AEST transitions. Mitigation: `OpenNowPill` caps `setTimeout` at 15 minutes (`OPEN_NOW_MAX_TIMER_MS`), bounding worst-case staleness to that window. A dedicated DST test (`isOpenNow` against UTC instants spanning 2024-10-06 spring-forward) verifies status correctness on both sides and at the AEDT-begin moment. **(4) `src/lib/reservationLinks.ts`** — provider detection from `reservation_link` URL: `tabelog` / `tablecheck` / `hotpepper` / `opentable` / `thefork` / `official` (any other domain) / `phone` (`tel:` URLs). Returns `{ primary, all, status }` where status is `reservable` (URL with provider) | `walk_in` (curated boolean) | `not_needed` (cafes/bakeries by default) | `unknown`. Phone fallback dedups against `tel:` reservation_links via `normalizePhone` (strips spaces, parens, dashes) so a venue with `reservation_link: "tel:+61264561420"` and `phone: "(02) 6456 1420"` doesn't render twice. Helper is built and tested but not yet consumed in EatCard's compact ActionRow (which keeps the generic "Reserve" label) — staged for Sprint 4 `EatDetailSheet` upgrade to provider-aware "Reserve on Tabelog" labels. **(5) `src/pages/town/TownEat.tsx`** — page composition: header (region/town byline + LiveBadge + "N OPEN NOW" amber pill via `useOpenNowCount`) → quick "Open now" Clock chip CTA above the filter bar (sets `?open=1`) → sticky `EatFilterBar` (`stickyTop={64}`) → list-only view with the toggle slot rendered as a static "List" affordance to reserve the visual rhythm slot for Sprint 3.6's deferred Map view → `EatCard` grid OR two empty states (filtered-zero with "Clear all filters" vs town-empty `EmptyStateCard`). 60-second outer `setInterval` re-runs the sort/filter so open-now positions stay accurate even between `nextChange` re-renders. Same `?eat={id}` standalone-Sheet deep-link pattern as Sprint 2's `?stay={id}` (single page-level controlled `<Sheet>` resolves the open eat from the URL — survives filtered-out and view=map states). Wired into `TownLayout.tsx` `/eat` route, replacing the live-Google `TownPlaces kind="eat"` branch (which is now dead code, deferred-cleanup until Sprint 4). **Test infrastructure**: pnpm scripts `test:openNow` (22 tests, includes DST + midnight regression) and `test:reservation` (14 tests) using tsx + node:assert (no vitest dependency added). **Validation**: typecheck = 19 baseline (zero new — all 19 pre-existing in `regions/yamanouchi/pages/*`), `data:smoke` PASS (107 stays / 121 eats), all three test suites green (22 + 14 + 21 = 57). **Architect verdict**: PASS, no SEVERE blockers, one non-blocking gap (DST scheduling drift) addressed with the 15-min timer cap. **Verified live**: `/snowy-mountains/jindabyne/eat` shows 30 cards with "8 OPEN NOW" badge (Bacco/Brumby render "Open · until midnight" post-fix); `/yamanouchi/yudanaka/eat?open=1` deep-link shows green chip + "1" filter count + "Clear all" pill + "4 of 25 shown".

- **Wired Stay page (Sprint 2, Prompt 2.6):** `artifacts/feelzlike/src/pages/town/TownStay.tsx` is now fully wired to the curated 228-entry dataset (no more Google Places — that pattern was retired in Prompts 2.1-2.5). Three new shared modules support it: (1) `src/lib/mountainScore.ts` extracts the `scoreMountain()` algorithm + `clamp()` helper out of `pages/region/TodaysCall.tsx` so both Today's Call AND the Stay page's "Today's #1" badge use the identical 0-100 composite (snow 40 / wind 25 / temp 20 / cloud 15 in winter, flattened in green) — no drift risk. (2) `src/lib/urlState.ts` centralises kebab↔snake slug conversion (`regionIdToSlug`/`townIdToSlug`/`mountainIdToDriveKey`) plus a `setUrlParam(key, value|null)` helper and a `useUrlParam(key)` hook that subscribes to `popstate` so multiple components on a page can share URL state without prop-drilling. The kebab→snake conversion is mandatory because `BaseTown.id` is kebab (`shibu-onsen`) but the curated `TownSlug` is snake (`shibu_onsen`) — the conversion is the single point that bridges those two namespaces. (3) `src/hooks/useTodaysWinner.ts` returns the top-scoring mountain for the current region with its `driveKey` already snake-cased and the parent-id rollup applied — so when Okushiga Kogen wins, the Stay page sorts by drive time to the parent `shiga_kogen` curated key (the only key present in `stay.drive_min_to_each_mountain`). The hook returns `{ winner: null, isLoading, error }` rather than throwing, so the Stay page degrades gracefully when weather data is unavailable. **Page composition**: header (region/town byline + LiveBadge + "Today's #1" amber-pill badge with score) → sticky `StayFilterBar` (`stickyTop={64}` to clear the AppShell topbar) → List/Map view toggle (`?view=map` URL state) → either `StayCard` grid OR `StayMap` (both receive `topMountainDriveKey` for sort/colour-bucketing) → secondary `StayPlatformBar` (variant=banner) for cross-shopping. Two empty states: filtered-zero ("No stays match these filters" with a "Clear all filters" button that round-trips through `setUrlParam(key, null)` for each filter key) vs town-empty (curated-data gap → `EmptyStateCard` with 7-day ETA + the platform bar). **Prereq patches** to make the wiring possible: `StayFilterBar.setUrlSearch` now preserves non-filter URL keys (introduced `FILTER_PARAM_KEYS` allowlist of 14 owned params — `type/price/sort/dry/ski/pet/self/kmThredbo/kmSkitube/onsen/tattoo/meal/en/walk` — and copies through everything else, fixing the latent bug where toggling a filter would have wiped `?view=map` and `?stay=...`). `StayCard` got optional controlled-mode props `{open, onOpenChange}` (Radix Sheet pattern — when omitted runs uncontrolled, when provided the parent owns state); `TownStay` uses this to URL-sync the open detail sheet via `?stay={stayId}` so deep-links land on a card with its sheet pre-opened. `TodaysCall.tsx` was refactored to import `clamp + scoreMountain` from the shared module (its 130 lines of duplicated scoring code is gone). **Region guard**: any stay whose `region` field doesn't match the page region is dropped with a `console.warn` — defence-in-depth against the same class of bug that surfaced as the Cooma Coaches transport leak in v0.3. **Verified live across all 6 towns**: jindabyne (25 stays, sorted by drive to Selwyn 78/100), berridale (12), cooma (18, with `?type=lodge,apartment` filter URL → "Showing 3 of 12"), yudanaka (21, sorted by drive to Yomase Onsen 93/100), shibu-onsen (22 — kebab→snake conversion verified), yomase (9). Map view confirmed showing "DRIVE TIME → SELWYN" colour-bucket legend with green/yellow/orange/red buckets. Typecheck remains at the 19-error baseline (zero new errors introduced). **Architect-mandated post-review fixes applied**: (a) added `setUrlParams(updates)` plural helper to `urlState.ts` for atomic single-write batch updates — "Clear all filters" now does ONE `replaceState` + ONE `popstate` for all 14 keys, instead of 14 separate render cycles + back-stack churn (`setUrlParam` was kept as a one-key thin wrapper). (b) the `?stay={id}` URL state was being served only by the per-card controlled-mode `StayCard`, which meant the deep-link silently no-op'd when the target stay was filtered out OR when the user was in map view (the architect caught this — the `openStay` resolver was computing a stay that nothing rendered). Refactored to a **single standalone controlled `<Sheet>` at page-level** mounting `StayDetailSheet` from the `openStay` lookup — same pattern `StayMap.inner.tsx` uses. List cards reverted to Radix uncontrolled mode for click-to-open (their job is just opening the sheet via Radix's own state machine). Verified: deep-linking `?type=hostel&stay=lake-crackenback-resort` opens the sheet even though Lake Crackenback (a Lodge) is hidden by the Hostel filter; deep-linking `?view=map&stay=lake-crackenback-resort` opens the sheet over the map. The `useEffect` that clears stale `?stay=` for ids not present in the town's data still runs as a defensive cleanup.

- **Affiliate deep-link helper (Sprint 2, Prompt 2.5):** Built `artifacts/feelzlike/src/lib/affiliateLinks.ts` as the single source of truth for accommodation booking URLs. **Provider union** of 10: booking_com, agoda, expedia, hotels_com, trip_com, airbnb, jalan (JP-only), rakuten (JP-only), tripadvisor (search-only — TAP uses Referer-based tracking, no URL param), official (uses `stay.website`). **Country-coverage matrix** (`PROVIDER_COUNTRIES`) automatically filters JP-only providers off AU stays and vice versa, even when affiliate IDs are forced via override. **Affiliate IDs** read defensively from `import.meta.env.VITE_<NAME>_AFFILIATE_ID` (try/catch around `import.meta` so node-tsx test env returns null) — set in Replit Secrets when each programme approves. Per-provider URL keys: `aid` (booking), `cid` (agoda), `affcid` (expedia + hotels.com), `allianceid` (trip.com), `c` (airbnb), `afid` (jalan), `scid` (rakuten). **HYBRID link policy** (architect-mandated, fixes initial search-URL-always design): `buildBookingLinks(stay)` returns ONLY providers whose curated URL exists in `stay.booking_links` (or `stay.website` for `official`) — preserves Prompt 2.2 UI parity (a stay never shows a "Book on Booking.com" button when the data team didn't curate that property there); affiliate IDs are injected onto the curated URL via `appendIfMissing` (won't double-write a key the curated URL already contains, e.g. preset `aid=PRESET`). Opt-in `buildBookingLinks(stay, { discoverAll: true })` adds search URLs (`?ss=` / `?q=` / `?searchKeyword=` / `/s/{q}/homes`) for every applicable provider — for future "find this property elsewhere" surfaces. JP-only providers prefer `name_local` (kanji) for the search query so jalan.net / travel.rakuten.co.jp searches hit the JP catalogue; global providers always use the romanised name. **Test override hatch**: optional 2nd arg `{ idsOverride: Partial<Record<Provider, string|null>> }` lets tests inject fake IDs without touching the module-level `AFFILIATE_IDS` (frozen at import). **Tests** at `src/lib/__tests__/affiliateLinks.test.ts` (21 cases) — uses `tsx + node:assert/strict` matching the existing `data:smoke` pattern (no vitest installed in workspace; same path as playbook spec — different runner). Coverage: default-mode curated-only filtering, discoverAll mode, country gating, ID injection on both curated + search URLs, per-provider param keys, double-write prevention, JP kanji preference + romanised fallback, missing-website handling, helper API surface (`hasAffiliateId`, `affiliateStatus`, `AFFILIATE_IDS`). Run with `pnpm --filter @workspace/feelzlike test:affiliate`. **Wired into** `StayCard.tsx` (replaced inline `PROVIDER_BRAND` + `PROVIDER_ORDER` + direct `stay.booking_links` read with imports + `buildBookingLinks(stay)`; "Official" button now reads `links.official` instead of `stay.website` directly for single-source purity) and `StayMap.inner.tsx` (replaced inline `POPOVER_PROVIDER_ORDER` + `labelMap` with `buildBookingLinks` + `PROVIDER_LABELS`; popover primary "Book" button still follows Booking → Agoda → Expedia → Hotels → Trip → Airbnb → Jalan → Rakuten preference, falls back to `links.official`). Architect review (`evaluate_task` with `includeGitDiff`) flagged ONE HIGH (search-URL-always misleads users → fixed by hybrid policy with curated-only default) and ONE MEDIUM (`stay.website` direct read → fixed by routing through `links.official`); both addressed and re-tested. Typecheck adds zero new errors (19 pre-existing baseline preserved).

- **Drive-time-coloured StayMap (Sprint 2, Prompt 2.4):** Built `artifacts/feelzlike/src/components/StayMap.tsx` (lazy Suspense wrapper) + `artifacts/feelzlike/src/components/StayMap.inner.tsx` (heavy leaflet map). The wrapper uses `React.lazy(() => import("@/components/StayMap.inner"))` so the leaflet runtime + react-leaflet only ship in the JS bundle when a page actually mounts a map — first-load JS for `/region/town` pages stays small, with a `Loader2` spinner shown by Suspense during the 100-200ms chunk fetch. **Marker colour-bucketing** by drive-min to today's #1 mountain per playbook spec: `≤15 min → green (#16a34a)`, `15-30 → yellow (#ca8a04)`, `30-45 → orange (#ea580c)`, `45+ → red (#dc2626)`, missing → gray. Drive-min resolves via `driveMinFor(stay, topMountainDriveKey)` which prefers `stay.drive_min_to_each_mountain[topKey]` (the snake_case curated key like `thredbo`/`shiga_kogen` — NOT the kebab-case RegionConfig mountain id, see Prompt 2.3 scratchpad note) and falls back to `stay.drive_min_to_nearest_mountain`. **Marker icons** are Leaflet `divIcon`s (HTML/SVG, not raster pngs) — coloured circle with a per-`stay.type` SVG path punched in white (12 type icons mapped: hotel/ryokan/lodge/apartment/airbnb/hostel/motel/cabin/bnb/minshuku/guesthouse/resort). divIcon avoids needing image files and renders crisp at any zoom. **Click → Leaflet popup** with first photo (or "No photo" placeholder), tone-coded type chip + `PriceBand` dots, name + name_local byline, drive-time label (e.g. "12 min → Thredbo" or "~25 min from you (estimate)" when locate-me active), "View details" Button (opens Sheet), and a primary "Book" anchor (Booking → Agoda → Expedia → Hotels.com → Trip → Airbnb → Jalan → Rakuten preference, falls back to `stay.website` if no provider, target=_blank with `rel="noopener noreferrer sponsored"`). To open the same Sheet content as a `StayCard` click, **`StayDetailSheet` is now exported from `StayCard.tsx`** (single-line change — `function` → `export function`) and the StayMap controls a single `Sheet` with `openStayId` state — clicking "View details" on any popup opens that stay's detail Sheet. The Radix Sheet has a hidden `SheetTrigger` to satisfy its focus-return contract while still being controlled. **Tile layer** picks up Mapbox if `VITE_MAPBOX_TOKEN` is set in env (`https://api.mapbox.com/styles/v1/mapbox/light-v11/...`), otherwise falls back to OpenStreetMap via Carto voyager (`{s}.basemaps.cartocdn.com/rastertiles/voyager/...`) — same tile style used by the existing JP weather map elsewhere in the app, no API key needed. Attribution string adapts to the active source. **"Locate me" button** uses `navigator.geolocation.getCurrentPosition` with `enableHighAccuracy:false, timeout:10s, maximumAge:60s`. When user position is known, markers recolour using haversine distance × 0.8 km/min heuristic (clearly labelled "estimate" in the popup so users know it's not a routed time). The geolocation success/error callbacks check an `isMountedRef` so they don't `setState` on an unmounted component if the user navigates away during the up-to-10s wait (architect MEDIUM 1). **Mobile/desktop chrome** — `zoomControl={!isMobile}` hides Leaflet's +/- buttons on mobile to save space; locate-me button anchors top-right on desktop, bottom-left on mobile (avoids the system bottom nav). `isMobile` is an optional prop that auto-detects via `useIsMobile()` (768px) when omitted — explicit prop wins for tests/SSR/canvas demos (architect MEDIUM 3). **Layout** — full-bleed on mobile (`h-[calc(100vh-4rem)]`), 80vh on desktop with `rounded-2xl + border` chrome (full-bleed on mobile is intentional — playbook spec). The 40/60 side-by-side with the list is the consumer page's job in Prompt 2.6, not this component. **Auto-fit bounds** — `MapBootstrap` runs `map.fitBounds(stayCoords, {padding:[40,40], maxZoom:14})` on mount and calls `map.invalidateSize()` after a 120ms timeout to handle the "grey tiles" Leaflet bug from late layout sizing. Bounds skip if fewer than 2 stays (Leaflet would throw); single-stay maps centre on that stay. **Centroid** — when no stay has lat/lng, falls back to caller-provided `fallbackCenter` (passed as the town's `RegionConfig.towns[].lat/lng`); without it, the map renders at `[0,0]` mid-Atlantic — JSDoc warns the caller (architect MEDIUM 4). **Composite marker keys** `${stay.id}-${idx}` — the JP curated dataset has 3 stays (`shimaya-ryokan`, `aburaya-tousen`, `wafu-no-yado-masuya`) listed in BOTH `yudanaka/stays.json` AND `shibu_onsen/stays.json` (the towns are adjacent in the Yudanaka–Shibu onsen-village complex; this is a curation duplication worth fixing in the dataset, not a code bug). The composite key prevents React duplicate-key warnings without masking the underlying dup. **Markercluster** — playbook spec calls for `leaflet.markercluster` if stay count > 50; current per-town counts are 25/12/18 NSW + 21/22/9 JP, all comfortably under 50. Region-level views (e.g. the demo) load 55+52 but those aggregate views aren't the production consumer (Prompt 2.6 will mount StayMap per-town). Skipping the install for now — flagged TODO in component for future when AU dataset expands. Demo at `__demos__/StayMap.demo.tsx` renders both regions with top-mountain toggle pills (4 NSW mountains, 6 JP mountains) for live recolour verification; visual confirms colour buckets are geographically correct (Thredbo village green, Charlotte's Pass yellow, Jindabyne orange, Cooma/Berridale red). Architect review (no CRITICAL/HIGH, 4 MEDIUM); 3 fixes applied (geolocation unmount guard, isMobile auto-detect fallback, fallbackCenter JSDoc); MEDIUM 2 (speculative leaflet+Radix focus quirk) skipped pending real-world report. Typecheck zero new errors (19 baseline).

- **Region-aware StayFilterBar (Sprint 2, Prompt 2.3):** Built `artifacts/feelzlike/src/components/StayFilterBar.tsx` — a sticky, region-aware filter bar that adapts its control set based on whether the user is in a NSW or JP town. Universal pills (Type / Price / Sort) render inline on desktop and inside a single Sheet on mobile (`useIsMobile` switches between layouts at 768px). Type chips are hard-limited to the 6 playbook-named types (Hotel/Ryokan/Lodge/Apartment/Airbnb/Hostel) and further filtered to types actually present in the supplied dataset (so AU's lack of ryokan correctly hides the chip; JP's lack of apartment hides that one). Price chips are 4-state multi-select ($/$$/$$$/$$$$). Sort dropdown has 5 options with default `drive_top` ("Drive time to today's #1 mountain"). NSW-only "More filters" Sheet exposes drying-room/ski-storage/pet-friendly/self-contained checkboxes plus distance-to-Thredbo and distance-to-Skitube/Perisher sliders (0–100km, "Any" when at max). JP-only Sheet exposes Onsen (Public/Private/Both), Tattoo policy (Allowed/Private only/Not allowed), Meal plan (where "Half-board (kaiseki)" matches both `half_board` AND `kaiseki` enum values per playbook bundling), English-spoken (Fluent / Limited-or-better which matches `yes` OR `limited`), and walk-to-Yudanaka-Station slider (0–30 min). **URL sync**: filters serialize to compact query params (`type=hotel,airbnb&price=2,3&sort=name_asc&dry=1&kmThredbo=20&onsen=public`) — defaults are omitted to keep URLs clean; price bands encode as 1-4 digits not `$$$` symbols. Read-side uses wouter v3.3.5 `useSearch()`; write-side uses `history.replaceState` + `dispatchEvent('popstate')` instead of the playbook-suggested `useLocation` to bypass base-prefix concerns and work with any future router base config. Filtered links are fully shareable — reload restores filters. **Result count** "Showing N of M stays" rendered inline (right-aligned), with optional `resultCount` prop override so consumer pages can avoid double-computation. **Active filter chips** row beneath the bar, each removable via X click, plus "Clear all" reset button. Active chips are gated by region — a stale `?onsen=public` carried over from a JP link won't render as a chip on the AU page (its underlying control isn't visible in the NSW Sheet either). Sort dropdown intelligently degrades when `topMountainDriveKey` is absent: the disabled `drive_top` item gets a "(no data)" suffix and the trigger visually displays `drive_nearest` to avoid showing a disabled item as the active value. **Exported helpers** for consumer pages: `applyStayFilters(stays, filters): Stay[]` and `applyStaySort(stays, sort, { topMountainDriveKey }): Stay[]` — Prompt 2.6's Stay page can re-run the same filter pipeline without coupling to component internals. `parseFiltersFromSearch` / `serializeFiltersToSearch` / `getActiveFilterCount` / `DEFAULT_FILTERS` also exported. The `onChange(filters)` callback is dedup-guarded via a `lastEmittedRef` (compares serialized form) so identical URL syncs from popstate echoes don't cause spurious parent re-renders. **Demo at `__demos__/StayFilterBar.demo.tsx`** renders the bar against both regional datasets stacked, with filtered StayCard grids below — used during build to verify sort+filter+URL-sync end-to-end (URL `?type=hotel,airbnb&price=2,3&sort=name_asc` correctly produced 12-of-55 AU + 9-of-52 JP filtered grids; cross-region URL `?onsen=public&dry=1&kmThredbo=20` correctly produced 2-of-55 AU with NSW chips only and 43-of-52 JP with onsen chip only). Architect review PASS with three fixes applied (onChange dedup, region-gated chip rendering, sort fallback display). Typecheck zero new errors (19 baseline). Pre-existing `lib/api-zod` dist staleness unchanged. **TODO note**: motel/cabin/bnb/minshuku/guesthouse/resort entries in the dataset are not surfaced as filter chips per playbook spec (chip set is hard-limited to the 6 named types) — they remain in the listing but are unfilterable. Worth revisiting when the AU dataset expands beyond Snowy Mountains base towns.

- **Region-aware StayCard + 4-card demo (Sprint 2, Prompt 2.2):** Built `artifacts/feelzlike/src/components/StayCard.tsx` — a single component that renders both AU lodges/hotels/apartments and JP ryokan/onsen properties off the discriminated `Stay` Zod union (branches gated on `stay.country`, no `as never`/`any` casts). Layout: Embla photo carousel at top with hover-revealed prev/next controls (falls back to a stylised lucide `Image` placeholder both when `photos[]` is empty AND when individual image loads fail via per-frame `onError` state); type badge (RYOKAN / HOTEL / LODGE / APARTMENT / AIRBNB / HOSTEL / MINSHUKU / GUESTHOUSE / RESORT / MOTEL / CABIN / B&B) tone-coded; price-band as 4 filled circles with `role="img"` + descriptive aria-label; name + `name_local` byline; `line-clamp-1` short description; drive-time chips picking the top 2 mountains by ascending minutes from `drive_min_to_each_mountain` (falls back to `nearest_mountain` + `drive_min_to_nearest_mountain` when the per-mountain map is absent); region-specific `FeatureBadges` (AU: drying_room/ski_storage/self_contained/pet_friendly with Flame/Snowflake icons; JP: onsen with public+private detection, tattoo_policy with ok/warn/neutral tone, meal_plan including Kaiseki, yukata_provided); `EnglishIndicator` (JP only, EN: Fluent/Limited/None). Booking row maps over a fixed `PROVIDER_ORDER` (booking_com → agoda → expedia → hotels_com → trip_com → airbnb → jalan → rakuten) rendering only providers with non-null `booking_links[provider]`, brand-coloured per playbook palette (Booking #003580, Agoda #5392f9, Airbnb #FF5A5F, Expedia #fcc917 + black ink, Hotels.com #d22630, Trip.com #287dfa, Jalan #ff6600, Rakuten #bf0000), each with `aria-label="Book {name} on {provider}"` and `rel="noopener noreferrer sponsored"`; an outline `Official` button renders when `stay.website` is set (uses an inline globe SVG to keep the import surface small). Booking row is a sibling of the Sheet trigger and stops click propagation so individual links are independently focusable. Whole-card-clickable pattern: photo carousel + booking row sit OUTSIDE a `SheetTrigger asChild` button (avoids `<button>`-in-`<button>`); the trigger button wraps title/desc/chips/badges. The `StayDetailSheet` shows full carousel, long_description, all feature badges, English indicator, every mountain drive time (full `drive_min_to_each_mountain` map, not just top 2), full amenities list, address with `Copy` button (Clipboard API + 1.5s "Copied" feedback), `Open in Maps` (Google Maps universal `search/?api=1&query=…&center=lat,lng` deep-link working on iOS/Android/desktop), `tel:` button, and full-label booking buttons with a "From $— · prices coming soon" placeholder for future price scraping. Affiliate URLs are taken verbatim from curated `booking_links` for now — Prompt 2.5 will wrap each href in the `affiliateLinks` helper to inject affiliate ids without changing the rendering contract. Demo at `artifacts/feelzlike/src/components/__demos__/StayCard.demo.tsx` resolves the 4 playbook-named records (`shibu_onsen/yorozuya`, `yudanaka/hotel-issa-tei-biyu-no-yado`, `jindabyne/lake-crackenback-resort`, `berridale/cottonwood-lodge-berridale`) via `getStaysByTown(town).find(id)` and renders a 1/2/4-column responsive grid with a `role="alert"` inline fallback if a record is missing. Visually verified at a temporary `/__demo/stay-card` route (since removed). Architect review (`responsibility: evaluate_task`, includeGitDiff): **PASS**, one nit fixed (replaced `links[id]!` non-null assertion in `BookingButtons` with an explicit `if (!href) return null` guard). Typecheck adds zero new errors.

- **Stay/Eat OpenAPI contract + generated React Query hooks (Sprint 2, Prompt 2.1):** Added 4 new endpoints to `lib/api-spec/openapi.yaml` describing the curated Stay/Eat dataset — `GET /regions/{region}/towns/{town}/stays`, `…/stays/{stayId}`, `…/eats`, `…/eats/{eatId}` — plus `stays`/`eats` tags, 4 path parameters (`StayEatRegionPath`, `StayEatTownPath`, `StayIdPath`, `EatIdPath`), and a full schema set mirroring `src/types/stayEat.ts`: `StayEatRegionSlug` (enum: snowy_mountains/yamanouchi, underscore-separated to match the on-disk JSON, distinct from the kebab-case `RegionId` used by live weather APIs), `TownSlug` (6-town enum), `PriceBand`, `EnglishSpoken`/`YesNo`/`YesLimitedNo` reusable enums, `StayBookingLinks`, `EatHours`, `StayBase`/`EatBase`, country-specific `StayAU`/`StayJP`/`EatAU`/`EatJP` extending the base via `allOf`, and `Stay`/`Eat` as `oneOf` discriminated unions on `country`. Orval codegen produces typed React Query hooks (`useGetTownStays`, `useGetTownStay`, `useGetTownEats`, `useGetTownEat`) plus query-key helpers (`getGetTownStaysQueryKey`, etc.). `src/hooks/useStayEat.ts` was refactored to keep its existing public API (`useStays`/`useEats`/`useStaysByRegion`/`useEatsByRegion`/`useAllStays`/`useAllEats`, plus new `useStay`/`useEat` single-record hooks) but now uses the generated query keys (`getGetTownStaysQueryKey(getRegionOfTown(town), town)`) for cache-key consistency with future API-backed migration, while keeping `queryFn` calling the static `@/data` loader — when the API server adds real handlers later, the migration is just dropping the `queryFn` override and switching to `useGetTownStays` directly. `lib/api-zod` dist remains stale due to pre-existing duplicate-export debt in `lib/api-zod/src/index.ts` (TS2308) — orphan, blocks dist rebuild only for that one package; `lib/api-client-react` dist rebuilds clean. Typecheck on the feelzlike app introduces zero new errors (only pre-existing TS debt: missing `queryKey` on TanStack v5 calls, `RegionId|undefined` string assigns in RegionOverview/TodaysCall/TownHome/TownCams/TownRoads, `Dashboard.topSnowResort`, `LanguageContextValue.lang`, `resorts undefined`).

- **Personalised Today's Call (Sprint 4, Prompt 4.1):** Mountain rankings on `/[:region]/today` are now skill/discipline/priorities/risk-tolerance aware via a localStorage-backed UserProfile. Schema lives at `artifacts/feelzlike/src/types/profile.ts` (`UserProfile = { skill_level: beginner|intermediate|advanced|expert; discipline: ski|snowboard|both; priorities: Priority[] capped at 3; risk_tolerance: low|medium|high; onboardedAt: ISO|null; v: 1 }`) with companion `MountainTags` interface (5 optional booleans: `beginner_friendly` / `expert_only` / `kids_lessons` / `terrain_park` / `backcountry_access`). Tags are populated on every mountain in `regions/{snowy-mountains,yamanouchi}.ts` via the `MountainLink` interface in `lib/feelzlike-shell/src/types.ts` — sourced from official trail maps + resort positioning copy. `useProfile()` hook (`src/hooks/useProfile.ts`) is SSR-safe (returns defaults until first useEffect), schema-versioned (drops records on `v` mismatch), cross-tab sync'd via `storage` events, and exposes `{ profile, setProfile, resetProfile, hasOnboarded, isLoaded }`. Personalisation engine `scoreMountainPersonalised()` (`src/lib/personalisedScore.ts`) wraps the existing pure `scoreMountain()` — preserves baseTotal unchanged and applies additive modifiers per playbook spec: skill match (beginner+20/-30, expert+20/-10), priorities (powder doubles snow sub-score capped at +40, park+10, backcountry+12, family+15/-8), risk (low: -25 wind>35km/h + -15 cloud>80%; high: +5 send-it). Returns `PersonalisedMountainScore extends MountainScore` adding `baseTotal: number` and `modifiers: ScoreModifier[]` (sorted by `|delta|` desc so UI can pick top 1-2 reasons). `groomers`/`value`/`crowds` priorities are intentional v1 no-ops (data not available) — kept in the type so onboarding captures intent. Companion `buildWhyCopy(score, "en"|"ja")` renders top 2 positive modifiers joined with " + " (reasons must NOT contain " + " internally — caught by the test suite's split-count assertion). UI: `ProfileSheet` component (`src/components/ProfileSheet.tsx`) handles both onboarding (4-step wizard with progress bar + Skip-for-now CTA that stamps `onboardedAt` so we never re-prompt) and edit (single-page form with Reset to defaults + Save). On first `/today` visit the onboarding sheet auto-opens via `useEffect` once `isLoaded && !hasOnboarded`. Header gains a sky-blue "personalised for you" pill badge (✨ icon, only when `hasOnboarded && personalised`), an "Edit profile" / "Personalise" button (label depends on hasOnboarded), and a "Show non-personalised" checkbox toggle that flips the matrix back to a neutral profile (empty priorities + medium risk + intermediate) — short-circuits all modifier branches without changing the call signature. Each row in the matrix gets a sky-blue "Why #1 for you: …" or "Why for you: …" line on desktop + mobile, rendered from `buildWhyCopy()`. **Critical guardrail:** the Replit cartographer plugin injects `data-replit-metadata` attributes into JSX which collides with explicit generic JSX syntax (`<Component<T> ...>`) — produces a babel parse error. ProfileSheet uses prop-typed callbacks (`onChange={(v: SkillLevel) => …}`) instead of explicit generics so TS still infers correctly without the babel collision. Test suite at `src/lib/__tests__/mountainScore.test.ts` (24 tests, all green) covers null-weather safety + each individual modifier rule + clamping + ordering + buildWhyCopy edge cases + an end-to-end "powder hunter vs family parent gets different #1" assertion using realistic mountain tags. Run via `pnpm --filter @workspace/feelzlike test:score` (uses `tsx --test` node:test runner). Typecheck baseline holds at 19. Stay page's `useTodaysWinner` deliberately stays on the base `scoreMountain` for v1 — accommodation browsers don't need ski-personalisation; that's a Sprint 5+ wire-up if it becomes important.

- **Sprint 4 differentiators 4.2–4.5 (Powder Window refinement, Webcams, Wind-hold, Powder Factor):** Built four mountain-page differentiator features sharing a single hourly-forecast pipeline. **(4.2 Powder Window)** Multi-window detection now buckets each upcoming snowfall event into GOLD (qualityScore≥75) / SILVER (≥50) / BRONZE grades with shimmer animation on GOLD bands; thresholds split by region — `POWDER_THRESHOLDS_AU = {minSnowfall:0.5, maxWind:25, minDuration:3, maxTemp:2}` for relaxed AU conditions, `POWDER_THRESHOLDS_DEFAULT = {1,20,3,2}` for Japow. `HourlyForecast.tsx` exports `GRADE_STYLES` reused by `PowderCalendar` (7-day glanceable strip on every mountain page) and `PowderSummaryCard` (cross-mountain ranking on `RegionOverview`). qualityScore formula: `totalSnow × (1/(1+avgWind/10)) × hours`, then clamped/scaled. Note: free-tier Open-Meteo has no hourly visibility, so the `>500m` playbook visibility threshold is treated as always-met with a TODO. **(4.3 Webcams)** Curated `data/webcams.ts` with verified JP cams across Yamanouchi+Iiyama (and AU keeps its existing API-driven grid). All curated entries default to `embedType:'external'` because almost every resort cam has hotlink/CORS protection — the branded fallback card linking to the resort's official cam page IS the product, not a degraded state. `MountainWebcams.tsx` provides grid + carousel + auto-rotate + click-to-modal with the fallback card surfacing prominently. **(4.4 Wind-hold)** Lift seed data at `data/lifts.ts` (~30 lifts AU+JP with `exposure: sheltered|moderate|exposed|highly_exposed`, `windThresholdKmh`, base/top elevation, type). `lib/windHoldPrediction.ts` exposes `predictLiftStatus(lift, hourly, resortElevationM)` which lapse-corrects wind to lift elevation (15% per 1000m gain), applies an exposure multiplier (sheltered 1.3x → highly_exposed 0.7x), and buckets the next 6 hours into `likely_open` (effective<0.7×threshold) / `possible_hold` (≤1.1×) / `likely_held` (>1.1×) with a confidence score and human-readable reason. `summariseMountainWindHold()` aggregates across all lifts at a mountain for the headline call. `LiftWindHoldPanel.tsx` renders an expandable list with bucketed badges, headline call, and a prominent "estimate only" disclaimer. 8 tests at `src/lib/__tests__/windHoldPrediction.test.ts`. **(4.5 Powder Factor)** `lib/powderFactor.ts` exposes `computePowderFactor(hourly, nowIso?, windowHours=48)` returning `{ score:0–100, quality, label/labelJa, reason/reasonJa, sub:{amount,temp,humidity,wind,freshness}, totalSnow, hoursSinceSnow, rainedAfterSnow, thawedAfterSnow }`. Distinct from Powder Window — Powder Window is forward-looking (when's the next dump?), Powder Factor is backward-looking (how good is the snow on the ground RIGHT NOW?). Algorithm only scores PAST hours within the 48h window; future hours are explicitly ignored. Sub-score weights sum to 100: amount 35 (asymptotic `35×(1-e^(-snow/15))` so 30cm≈32 not 100), temp 20 (sweet-spot curve peaking at -5.5°C), humidity 15 (lower=lighter snow), wind 15 (calmer=better), freshness 15 (decays from 15→0 over 48h since last snow hour). Destruction multipliers AFTER the last snow hour: rain detected → ×0.3 (icy crust), thaw above +2°C → ×0.5 (refrozen) — these stack in priority (rain wins). Quality buckets: bottomless≥88 / premium≥73 / soft_fast≥58 / decent≥40 / hardpack≥20 / survival<20 with bilingual labels (e.g. "Bottomless Japow" / "底なしジャパウ"). `POWDER_QUALITY_STYLES` exports tailwind classes per quality. `PowderFactorBadge.tsx` renders as a hero card with score dial + label + reason + click-to-expand sub-score breakdown (with disclaimer + destruction-warning row when rain/thaw detected). Wired into all 3 mountain pages immediately above `HourlyForecast` so the backward-looking snapshot pairs visually with the forward-looking strip. 11 tests at `src/lib/__tests__/powderFactor.test.ts` cover empty input, fresh-deep-cold dump, rain destruction, thaw destruction, freshness decay, future-hour exclusion, window-edge exclusion, wind penalty, humidity reward, reason copy, and a fuzz test (random hours never NaN/out-of-range). Test runner extended: `pnpm --filter @workspace/feelzlike test:all` runs all 44 tests across mountainScore (24) + windHoldPrediction (9) + powderFactor (11). Typecheck baseline holds at 19 (zero new errors — same pre-existing TS debt in `regions/yamanouchi/pages/resorts.tsx`).

- **Curated Stay+Eat dataset (v0.4, 228 entries):** Statically-imported, Zod-validated catalogue of 107 stays + 121 eats across all 6 base towns (Jindabyne 25/30, Berridale 12/13, Cooma 18/26, Yudanaka 21/25, Shibu Onsen 22/16, Yomase 9/11). Lives at `artifacts/feelzlike/src/data/curated/by_region/<region>/<town>/{stays,eats}.json`. `src/types/stayEat.ts` defines Zod discriminated unions on `country` (AU adds drying_room/ski_storage/pet_friendly/self_contained/distance_to_*; JP adds onsen/tattoo_policy/meal_plan/yukata_provided/walk_min_to_yudanaka_station; AU eats add apres_ski/takeaway/groceries; JP eats add vegetarian_friendly/kid_friendly). `src/data/index.ts` validates every JSON file at module load (throws fast on schema drift) and exports typed accessors `getStaysByTown / getEatsByTown / getStaysByRegion / getEatsByRegion / getAllStays / getAllEats / getTowns / getRegions`. `src/hooks/useStayEat.ts` wraps the accessors in TanStack Query hooks (`useStays(town)`, `useEats(town)`, region/all variants) with `staleTime: Infinity` since the data is static — same calling convention as API-backed hooks for trivial future migration. Runtime smoke at `scripts/curated-smoke.mts` (`pnpm --filter @workspace/feelzlike data:smoke`) re-validates and asserts counts. Vitest test scaffold in `src/data/__tests__/stayEat.test.ts` (vitest not yet wired). Known data gap: 10 stays (3 Berridale + 3 Cooma motels, 2 Yudanaka ryokan, 2 Yomase hotels) have `booking_links` keys present but all values null — tolerated up to 15 in the test, surfaced in smoke output.

## External Dependencies

- **Supabase:** Read-only connection for live snow, resort, and storm data (e.g., `yamanouchi_resorts_today`, `yamanouchi_storms_today`).
- **PostgreSQL (Replit):** Local database for `accommodation`, `dining`, `attractions`, `users`, and `subscriptions` data.
- **Open-Meteo (`api.open-meteo.com`):** Primary global forecast feed for `/api/regions` and per-town weather endpoints. Requires attribution and has a free-tier usage limit.
- **Booking.com Affiliate Program:** Integrated for accommodation bookings via affiliate ID `75fd40675e05769d549b60370a6455d3`.
- **Wikimedia API:** Used for fetching region card images on the master landing page.
- **Unsplash:** Fallback image source if Wikimedia API fails.
- **Anthropic AI:** Client integration for AI functionalities.