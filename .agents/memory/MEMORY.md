- [feelzlike brand voice](feelzlike-brand-voice.md) — lowercase, middot · required (NOT forbidden), no em/en dashes, no emojis, DIN Pro, sky/blue.
- [feelzlike snow-accent pink](feelzlike-snow-accent-color.md) — snow amounts+icons render #EC008C via --color-snow-accent; powder medals stay amber; detect via icon-identity locally / accent:"snow" in shared LiveConditions; band labels stay sky.
- [feelzlike radar stability](feelzlike-radar-stability.md) — never reintroduce a cross-Replit iframe as the primary radar; in-repo RadarMap with independent layers is the durable pattern.
- [feelzlike BOM radar coverage](feelzlike-bom-radar-coverage.md) — any AU point gets its own nearest covering BOM radar (not the nearest ski region); catalogue = only radars BOM serves a gif for; null coverage = no Official tab, honest copy.
- [feelzlike BOM radar animation](feelzlike-bom-radar-animation.md) — Official AU tab IS animated (composite of BOM layers+frames); frames are 10-min :X4 NOT 6-min; respect BOM whole-egress rate-limit (cache/dedupe/allowlist/stale); degrade still→link-out.
- [feelzlike map cross-region](feelzlike-map-cross-region.md) — interactive RadarMap shows ALL same-country towns/resorts (AU/JP grouped), not region-only; local REGION_COUNTRY duplicates registry on purpose.
- [feelzlike weather source fallback](feelzlike-weather-source-fallback.md) — non-BOM needs OWM fallback reshaped to Open-Meteo; serve-stale cache is load-shedding; /near-you degrades per-section.
- [feelzlike news + newsletter REMOVED](feelzlike-news-feeds.md) — on-site news feed + general email newsletter removed (routes/UI gone, DB tables dormant); snow/powder email alerts KEPT and now PREMIUM-gated; don't reintroduce news/newsletter without product sign-off.
- [feelzlike trip planner](feelzlike-trip-planner.md) — /plan = simple per-destination comparison snapshot (AU-only, ensemble per mountain id at mid-mountain, fail-soft per mountain); best-window ranker + travel-day layer were REMOVED by user request (over-complicated); don't reintroduce without sign-off.
- [feelzlike observation reconciliation](feelzlike-observation-reconciliation.md) — "sunny while raining" is model lag; fix is JP-only dry->wet JMA AMeDAS override w/ elevation-aware station pick, fail-soft.
- [feelzlike premium promo](feelzlike-premium-promo.md) — promo start+end (local-time parse, default ends 31 dec 2026); server `requireEntitlement` = real paywall; premium VISIBLE+usable, gate=pass-through (no lock), badge+subscribe live on /premium hub only.
- [feelzlike tsx test isolation](feelzlike-tsx-test-isolation.md) — pure logic needing `tsx --test` must NOT transitively import `@/regions` (PNG assets crash import); split pure file from catalog file.
- [feelzlike town slug convention](feelzlike-town-slug-convention.md) — kebab ids (urls) vs snake slugs (dataset keys); convert via urlState helpers, never `as TownSlug` cast (silent empty lookups).
- [feelzlike region readiness model](feelzlike-region-routing.md) — nav town-scoped + data-gated; region-level routes are direct-URL-only (redirect home, never stub); link town→region routes with the `~` escape or they silently hit the Not-found stub.
- [feelzlike geolocation funnel](feelzlike-geolocation-funnel.md) — NearYou never auto-prompts; denied = re-enable guidance + reload, never silent retry.
- [feelzlike security posture](feelzlike-security-posture.md) — SAST "secret" HIGHs are the public Supabase anon key + analytics key (false positives); app.ts is well-hardened; dep highs are dev/transitive only.
- [shell output token aliasing](shell-output-aliasing.md) — bash/rg/cat output in this env silently aliases some identifiers (NAV→ln, RegionStub→n); use the `read` tool for trustworthy content.
- [monorepo project-reference rebuild](monorepo-project-reference-rebuild.md) — after codegen/schema edits, `tsc` consumers fail "no exported member" until you `tsc -b` the lib decls, even though dev server works.
- [feelzlike location-weather endpoint](feelzlike-weather-endpoint-sharing.md) — single `/weather/:id` serves AU+JP; region-gate forecast_days (undefined region = AU); consumers slice-cap `daily`, audit for open-ended slices.
- [feelzlike affiliate (Awin+CJ)](feelzlike-awin-monetization.md) — LIVE links = places.ts (AU/JP/NZ), not legacy affiliateLinks.ts; CJ powers Booking.com+Hotels.com+trivago (per-advertiser 8-digit AID, no auto-convert), Awin powers Europcar; one network per merchant; all behind canUseAds; Yamanouchi booking migrated off its old direct aid onto CJ.
- [feelzlike touch gestures](feelzlike-pull-to-refresh-gestures.md) — PullToRefresh's top-of-page touchmove preventDefault stalls horizontal scrollers unless excluded; ScrollToTop must skip popstate.
- [feelzlike lift-panel honesty](feelzlike-lift-honesty.md) — NO live AU lift feed (lifts.ts all-closed hardcode); Phase-1 gates open/closed UI via hasLiveLiftStatus + panel liveStatusKnown; server snowDepth defaults 0 = false 'no snow' without the flag.
- [feelzlike lift URL rot + Thredbo feed](feelzlike-lift-honesty.md) — resort liftStatusUrl strings rot (sites restructure → old paths redirect to XML/404); must target human report pages, re-verify; Thredbo DOES publish a real per-lift XML feed (Phase-2 source).
- [feelzlike prerender flash](feelzlike-prerender-flash.md) — prod-only unstyled SEO snapshot flashed top-left before React mounts; fix = wrap in #seo-prerender + hide via inline CSS (noscript restores); never render a React element with that id.
- [feelzlike adding a country](feelzlike-add-country.md) — sync set in regions.ts header is incomplete (also transport REGISTRY throws-at-load + Countries.tsx); root `pnpm typecheck` short-circuits on pre-existing anthropic-lib error, so typecheck artifacts directly.
- [feelzlike scroll-to-top popstate](feelzlike-scrolltotop-popstate.md) — ScrollToTop must ignore synthetic Event("popstate") (only real PopStateEvent suppresses reset); else filter/urlState query dispatches leak a stale flag and the next forward nav skips scroll-to-top ("opens half way down").
- [feelzlike weather icons](feelzlike-weather-icons.md) — snow codes (71-77,85-86) render as Snowflake (not CloudSnow) to stay visually distinct from rain CloudRain; convention spans ~6 weatherCode->icon sites + dashboard that must stay in sync, order so 80-82 stays rain.
- [feelzlike deploy topology + Sentry noise](feelzlike-deploy-sentry-noise.md) — ARTIFACT-mode deploy (per-artifact artifact.toml; .replit run ignored, build=pre-build hook only); api-server probe=/api/healthz; Sentry 401=decoy; ~3s zero-log build fail=transient platform glitch, just re-publish.
- [feelzlike authMiddleware fail-soft](feelzlike-auth-middleware-failsoft.md) — DB-backed getSession runs before every /api route; unguarded, a cold-start DB hiccup 500s every logged-in request (incl public /alerts/subscribe). Must try/catch → next() anonymous. "HTTP 500 : INTERNAL_ERROR" = app's own global handler, not platform.
- [feelzlike snowmaking](feelzlike-snowmaking.md) — 2 honest layers (wet-bulb window + curated capability) across ALL AU+NZ resorts (Japan excluded); shared panel on both detail pages; all-weather only if a real snow factory; client-side, no backend.
- [feelzlike snow units](feelzlike-snow-units.md) — snowfall_sum is cm, precipitation_sum is mm; label snow cm + rain mm; they share daily cards so it's easy to mislabel snow as mm (10x understatement).
- [feelzlike home region fallback](feelzlike-home-region-fallback.md) — home region list = API REGIONS + hardcoded FALLBACK_REGIONS in Countries.tsx; keep in sync or regions silently vanish when api-server is slow/down.
- [feelzlike forecast chart y-axis](feelzlike-forecast-chart-axis.md) — ForecastChart YAxis: never use negative chart margin.left to tighten the axis (clips wide "km/h" wind labels into "m/h"); set explicit per-metric YAxis width instead.
- [feelzlike headline snow elevation](feelzlike-headline-snow-elevation.md) — headline snow at mid-mountain (mirrors server bandElevations().mid); EnsembleForecast must use the RESOLVED snowfallOutlookElevationM, not the requested mid, else fail-soft village headline pairs with a mid ensemble (two snow stories).
- [feelzlike generated docs](feelzlike-generated-docs.md) — 2 tsx scripts (api-server/src/scripts) render PR + 3 overview PDFs to exports/; MUST manually copy to feelzlike/public/downloads (direct-URL, no in-app links) + owner manual re-publish; verify every copy claim vs app (personalisation real, green-season only Thredbo summer, roads≠BOM/JMA).
- [feelzlike places search scoping](feelzlike-places-search-scoping.md) — searchText has NO includedRegionCodes; scope via ONE restricted searchText per country (locationRestriction rectangle, allSettled) + addressComponents country-code filter (JP box leaks Korea/China) + places.types placeLike ranking; NOT a global search/post-filter; client reconciles picked place→curated town (name<=25km / prox<=6km).
- [feelzlike prod HTML security headers](feelzlike-deploy-sentry-noise.md) — application router serves static HTML from the edge, so helmet/app.ts adds headers only to /api, never HTML; only `<meta name=referrer>` works in prod, the rest (nosniff/Permissions-Policy/X-Frame-Options) need a topology change.
- [feelzlike radar lazy-chunk crash containment](feelzlike-radar-stability.md) — wrap the lazy RadarMap Suspense in a local class boundary; retry must hard-reload (React.lazy caches rejected imports) so a chunk failure degrades inline instead of blanking the weather page.
- [feelzlike NZ live roads](feelzlike-nz-live-roads.md) — NZ = two tiers: LIVE Waka Kotahi (NZTA) approach-corridor events + SEASONAL access-road chain rule; cold outage must THROW (not return [], which UI reads as "all clear"); JP has no live feed, stays seasonal.
- [feelzlike skiable-now split](feelzlike-skiable-now-split.md) — powder medal = snow-incoming only; separate skiable-now chip (deriveSkiableNowRead wraps computeLiftOperationStatus); only a live lift feed asserts skiable; mirror each page's LiftWindHoldPanel exprs.
- [feelzlike ad kit](feelzlike-ad-kit.md) — QR ad assets generator (posters/stickers/boards print PDFs + 1920x1080/1080x1920 screen PNGs); landscape scale unit MUST be height-bound; transport-inclusive honest copy; owner assets, exports/ad-kit only.
- [feelzlike Google Analytics 4](feelzlike-google-analytics.md) — consent-gated GA4 (mirrors Awin); gtag stub MUST push real `arguments` (array = silent no-hit); pin stripped page_location in config or automatic hits leak alert ?token=; custom events flow via track()->gaEvent (one seam, skips page_view), never a direct gtag call.
