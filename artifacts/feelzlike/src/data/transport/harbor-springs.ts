import type { TransportProviderList } from "@/types/transport";

/** Harbor Springs (Michigan, USA) transport providers. Empty in this pass: no operator has been curated to the existing verification standard, so the Transport page renders its empty state rather than guessed timetables. */
export const HARBOR_SPRINGS_TRANSPORT: TransportProviderList = [];
