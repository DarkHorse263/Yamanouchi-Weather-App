import type { TransportProviderList } from "@/types/transport";

/** Rangeley (Maine, USA) transport providers. Empty pending verification. */
export const RANGELEY_TRANSPORT: TransportProviderList = [];
