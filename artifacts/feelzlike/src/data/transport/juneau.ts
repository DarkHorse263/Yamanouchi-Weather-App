import type { TransportProviderList } from "@/types/transport";
/** Juneau (Alaska, USA) transport providers. Empty pending operator verification; no guessed timetables. */
export const JUNEAU_TRANSPORT: TransportProviderList = [];
