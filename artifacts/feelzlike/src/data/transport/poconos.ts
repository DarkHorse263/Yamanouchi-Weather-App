import type { TransportProviderList } from "@/types/transport";
/** Poconos (Pennsylvania, USA) transport providers. Empty pending operator verification; no guessed timetables. */
export const POCONOS_TRANSPORT: TransportProviderList = [];
