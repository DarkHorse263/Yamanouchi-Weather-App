import type { TransportProviderList } from "@/types/transport";

/** Keweenaw Peninsula (Michigan, USA) transport providers. Empty in this pass: no operator has been curated to the existing verification standard, so the Transport page renders its empty state rather than guessed timetables. */
export const KEWEENAW_PENINSULA_TRANSPORT: TransportProviderList = [];
