import type { TransportProviderList } from "@/types/transport";

/** Highmount (New York, USA) transport providers. Empty pending verification. */
export const HIGHMOUNT_TRANSPORT: TransportProviderList = [];
