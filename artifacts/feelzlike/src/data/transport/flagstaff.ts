import type { TransportProviderList } from "@/types/transport";
/** Flagstaff (Arizona, USA) transport providers. Empty pending operator verification; no guessed timetables. */
export const FLAGSTAFF_TRANSPORT: TransportProviderList = [];
