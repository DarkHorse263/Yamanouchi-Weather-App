import type { TransportProviderList } from "@/types/transport";

/** Newry-Bethel (Maine, USA) transport providers. Empty pending verification. */
export const NEWRY_BETHEL_TRANSPORT: TransportProviderList = [];
