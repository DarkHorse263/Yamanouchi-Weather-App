import type { TransportProviderList } from "@/types/transport";

/**
 * Vancouver & the Island (BC, Canada) transport providers.
 *
 * Empty in this pass · the North Shore hills run their own shuttles and BC
 * Ferries / a Comox flight serve Mount Washington, but none has been curated
 * to the verification standard used for the AU/JP/NZ entries. The Transport
 * page renders its empty state rather than shipping guessed timetables.
 */
export const VANCOUVER_TRANSPORT: TransportProviderList = [];
