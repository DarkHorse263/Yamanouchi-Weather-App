import type { TransportProviderList } from "@/types/transport";
/** Wausau (Wisconsin, USA) transport providers. Empty pending operator verification; no guessed timetables. */
export const WAUSAU_TRANSPORT: TransportProviderList = [];
