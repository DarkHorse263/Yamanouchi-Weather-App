import type { TransportProviderList } from "@/types/transport";

/** Hunter (New York, USA) transport providers. Empty pending verification. */
export const HUNTER_TRANSPORT: TransportProviderList = [];
