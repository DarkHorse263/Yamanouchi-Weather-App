import type { TransportProviderList } from "@/types/transport";
/** High Country (North Carolina, USA) transport providers. Empty pending operator verification; no guessed timetables. */
export const HIGH_COUNTRY_TRANSPORT: TransportProviderList = [];
