import type { TransportProviderList } from "@/types/transport";

/**
 * Tasmania transport providers · Ben Lomond is reached almost entirely
 * by private car (the Jacobs Ladder switchbacks are the iconic final
 * climb). McDermott's Coaches has run charter shuttles from Launceston
 * in past winters but the service is not consistently scheduled, so we
 * don't claim it here. We never invent services · add verified
 * providers (bus, train, shuttle, taxi, rental_car) to this list as
 * they are confirmed for the coming season.
 */
export const TASMANIA_TRANSPORT: TransportProviderList = [];
