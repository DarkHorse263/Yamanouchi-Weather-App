import type { TransportProviderList } from "@/types/transport";

/** Lake-Placid (New York, USA) transport providers. Empty pending verification. */
export const LAKE_PLACID_TRANSPORT: TransportProviderList = [];
