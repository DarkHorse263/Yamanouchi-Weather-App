{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "japan:catalogue:generate": "pnpm run japan:reviews:validate && pnpm --filter @workspace/japan-ski-catalogue run generate",
    "japan:catalogue:generate:check": "pnpm run japan:reviews:validate && pnpm --filter @workspace/japan-ski-catalogue run generate:check",
    "japan:catalogue:validate": "pnpm run japan:reviews:validate && pnpm --filter @workspace/japan-ski-catalogue run validate",
    "japan:catalogue:report": "pnpm run japan:reviews:validate && pnpm --filter @workspace/japan-ski-catalogue run report",
    "japan:reviews:validate": "pnpm --filter @workspace/japan-ski-catalogue run reviews:validate",
    "ski:catalogue:generate": "pnpm --filter @workspace/ski-catalogue run generate",
    "ski:catalogue:validate": "pnpm --filter @workspace/ski-catalogue run validate",
    "ski:catalogue:test": "pnpm --filter @workspace/ski-catalogue run test",
    "canada:catalogue:generate": "pnpm --filter @workspace/canada-ski-catalogue run generate",
    "canada:catalogue:generate:check": "pnpm --filter @workspace/canada-ski-catalogue run generate:check",
    "canada:catalogue:validate": "pnpm --filter @workspace/canada-ski-catalogue run validate",
    "canada:catalogue:report": "pnpm --filter @workspace/canada-ski-catalogue run report"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  },
  "pnpm": {
    "overrides": {
      "brace-expansion@>=1.0.0 <1.1.12": "^1.1.12",
      "brace-expansion@>=2.0.0 <2.0.3": "^2.0.3",
      "path-to-regexp@<8.4.2": "^8.4.2",
      "picomatch@<4.0.4": "^4.0.4",
      "lodash@<=4.17.23": "^4.17.21",
      "fast-uri@<3.1.2": "^3.1.2",
      "yaml@<2.9.0": "^2.9.0",
      "postcss@<8.5.14": "^8.5.14",
      "ip-address@<10.1.1": "^10.1.1"
    }
  }
}
